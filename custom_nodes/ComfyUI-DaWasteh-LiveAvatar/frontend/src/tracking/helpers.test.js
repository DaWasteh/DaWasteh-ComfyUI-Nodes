import test from 'node:test';import assert from 'node:assert/strict';import {handInputs,loopController} from './helpers.js';
test('non-selfie Holistic maps matching hands',()=>{const r={leftHandLandmarks:'L',rightHandLandmarks:'R'};assert.deepEqual(handInputs(r),[['Left','L'],['Right','R']]);assert.deepEqual(handInputs(r,true),[['Left','R'],['Right','L']])});
test('tracking loop reports a rejected step and schedules another frame',async()=>{let callback,errors=0;const old=globalThis.requestAnimationFrame;const cancel=globalThis.cancelAnimationFrame;globalThis.requestAnimationFrame=f=>{callback=f;return 1};globalThis.cancelAnimationFrame=()=>{};const loop=loopController(()=>Promise.reject(new Error('bad')),()=>errors++);loop.start();await callback();loop.stop();globalThis.requestAnimationFrame=old;globalThis.cancelAnimationFrame=cancel;assert.equal(errors,1)});
